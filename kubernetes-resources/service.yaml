apiVersion: v1
kind: Service
metadata:
  name: react-tutorial-service
spec:
  type: NodePort
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: frontend-port
  selector:
    app: react-tutorial
